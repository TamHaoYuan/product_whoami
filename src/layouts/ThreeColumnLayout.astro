---
import BaseLayout from './BaseLayout.astro';
import Navigation from '@components/Navigation.astro';
import Sidebar from '@components/Sidebar.astro';
import MobileNav from '@components/MobileNav.astro';

export interface Props {
  title?: string;
  description?: string;
  image?: string;
  article?: boolean;
  toc?: {
    depth: number;
    slug: string;
    text: string;
  }[];
}

const { title, description, image, article, toc } = Astro.props;
---

<BaseLayout title={title} description={description} image={image} article={article}>
  <slot name="head" slot="head" />
  <div class="flex flex-col lg:flex-row min-h-screen bg-slate-50 dark:bg-slate-900">
    <!-- Mobile Navigation (shown only on small screens) -->
    <MobileNav />

    <!-- Left Sidebar (hidden on mobile) -->
    <Navigation class="hidden lg:flex lg:flex-col lg:w-60 lg:fixed lg:inset-y-0 shrink-0 bg-white dark:bg-slate-800 shadow-md" />

    <!-- Main Content -->
    <main class="flex-1 w-full px-4 pt-16 lg:pt-0 lg:ml-60 xl:mr-64">
      <div class="max-w-4xl mx-auto py-6">
        <slot />
      </div>
    </main>

    <!-- Right Sidebar (hidden on mobile and tablet) -->
    <Sidebar class="hidden xl:block xl:w-64 shrink-0 fixed right-0 top-0 bottom-0 bg-white dark:bg-slate-800 shadow-md" toc={toc} />
  </div>
</BaseLayout>